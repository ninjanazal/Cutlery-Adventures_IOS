import SpriteKit
import GameplayKit

// cena de jogo
class GameScene: SKScene {
    //MARK: Vars
    override func didMove(to view: SKView) {
        // define a cor de fundo padrao
        backgroundColor = UIColor.gray
    }
}
